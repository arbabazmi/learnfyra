name: Full Deploy — Dev

on:
  workflow_dispatch:

permissions:
  id-token: write   # required for OIDC token request
  contents: read

jobs:
  test:
    name: Tests (must pass before deploy)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

  deploy:
    name: Deploy to Dev
    runs-on: ubuntu-latest
    needs: test
    environment: dev

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Write Anthropic API key to SSM
        run: |
          aws ssm put-parameter \
            --name "/learnfyra/dev/anthropic-api-key" \
            --value "${{ secrets.ANTHROPIC_API_KEY_DEV }}" \
            --type SecureString \
            --overwrite \
            --region ${{ vars.AWS_REGION }}

      - name: Install app dependencies (needed by esbuild when bundling Lambdas)
        run: npm ci

      - name: Install CDK dependencies
        run: cd infra/cdk && npm ci

      - name: CDK Bootstrap (safe to re-run)
        run: |
          cd infra/cdk
          npx cdk bootstrap \
            aws://${{ vars.AWS_ACCOUNT_ID }}/${{ vars.AWS_REGION }} \
            --context env=dev
        env:
          CDK_DEFAULT_ACCOUNT: ${{ vars.AWS_ACCOUNT_ID }}
          CDK_DEFAULT_REGION: ${{ vars.AWS_REGION }}

      - name: Deploy CDK stack — dev
        run: |
          cd infra/cdk
          npx cdk deploy \
            --context env=dev \
            --context enableCustomDomains=true \
            --context rootDomainName=learnfyra.com \
            --context hostedZoneId=Z027880820G9W7LYJRE7H \
            --context cloudFrontCertificateArn=arn:aws:acm:us-east-1:796929287685:certificate/abb068ee-3a48-4546-bb30-b494d7fe1ea7 \
            --context apiCertificateArn=arn:aws:acm:us-east-1:796929287685:certificate/abb068ee-3a48-4546-bb30-b494d7fe1ea7 \
            --require-approval never \
            --outputs-file cdk-outputs.json
        env:
          CDK_DEFAULT_ACCOUNT: ${{ vars.AWS_ACCOUNT_ID }}
          CDK_DEFAULT_REGION: ${{ vars.AWS_REGION }}

      - name: Extract CDK outputs
        id: cdk-outputs
        run: |
          FRONTEND_BUCKET=$(jq -r '.["LearnfyraStack-dev"]["FrontendBucketName"]' infra/cdk/cdk-outputs.json)
          FRONTEND_URL=$(jq -r '.["LearnfyraStack-dev"]["FrontendUrl"]' infra/cdk/cdk-outputs.json)
          echo "frontend_bucket=$FRONTEND_BUCKET" >> $GITHUB_OUTPUT
          echo "frontend_url=$FRONTEND_URL" >> $GITHUB_OUTPUT

      - name: Sync frontend to S3
        run: |
          aws s3 sync frontend/ s3://${{ steps.cdk-outputs.outputs.frontend_bucket }}/ \
            --delete \
            --region ${{ vars.AWS_REGION }}

      - name: Print dev URL
        run: echo "Dev deployed at ${{ steps.cdk-outputs.outputs.frontend_url }}"
