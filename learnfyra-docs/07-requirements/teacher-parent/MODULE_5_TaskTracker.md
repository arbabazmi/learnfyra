# Module 5 — Task Tracker
## Teacher & Parent Roles

| Field | Value |
|---|---|
| Module | M05 — Teacher & Parent Roles |
| Version | 1.0 |
| Date | 2026-04-02 |
| Status | Draft |
| Prepared By | BA Agent |
| Source Document | MODULE_5_FRD_Teacher_Parent.md v1.0 |

---

## Infrastructure & Data Model Tasks

| Task ID | Layer | Title | Description | Depends On | Estimate | Priority |
|---|---|---|---|---|---|---|
| M5-INF-001 | Infrastructure | Provision LearnfyraClasses-{env} DynamoDB table | Create the Class table with PK = CLASS#{classId}, SK = METADATA, TeacherIndex GSI (PK: teacherId, SK: createdAt, ALL projection), and InviteCodeIndex GSI (PK: inviteCode, ALL projection). Enable point-in-time recovery on prod. | None | 3 | P1 |
| M5-INF-002 | Infrastructure | Provision LearnfyraAssignments-{env} DynamoDB table | Create the Assignment table with PK = ASSIGNMENT#{assignmentId}, SK = METADATA, ClassIndex GSI (PK: classId, SK: createdAt, ALL projection), and ClassDueDateIndex GSI (PK: classId, SK: dueDate, ALL projection). Enable PITR on prod. | None | 3 | P1 |
| M5-INF-003 | Infrastructure | Provision LearnfyraStudentAssignmentStatus-{env} DynamoDB table | Create the StudentAssignmentStatus table with PK = ASSIGNMENT#{assignmentId}, SK = STUDENT#{studentId}, StudentIndex GSI (PK: studentId, SK: assignmentId, ALL projection), and ClassAssignmentIndex GSI (PK: classId, SK: assignmentId, ALL projection). This table has two GSIs and must not be provisioned with only one. Enable PITR on prod. | None | 5 | P1 |
| M5-INF-004 | Infrastructure | Provision LearnfyraParentChildLinks-{env} DynamoDB table | Create the ParentChildLink table with PK = USER#{parentId}, SK = CHILD#{childId}, and InvertedIndex GSI (PK: childPK = USER#{childId}, SK: parentSK = PARENT#{parentId}, ALL projection). Enable PITR on prod. | None | 3 | P1 |
| M5-INF-005 | Infrastructure | Provision LearnfyraParentInviteCodes-{env} DynamoDB table | Create the ParentInviteCode table with PK = INVITE#{code}, SK = METADATA. Configure the ttl attribute (Number, Unix epoch) as the DynamoDB TTL attribute on the table. TTL is the sole cleanup mechanism — no scheduled job. No GSIs required. Enable PITR on prod. | None | 2 | P1 |
| M5-INF-006 | Infrastructure | Enable DynamoDB Streams on LearnfyraUserProgress-{env} | Verify that the UserProgress table CDK construct has StreamSpecification enabled (StreamViewType: NEW_AND_OLD_IMAGES). If not already present, add it to the M04 CDK construct and redeploy. This is required for Phase 2 email notifications without schema migration. | M5-INF confirmation from M04 CDK | 2 | P1 |
| M5-INF-007 | Infrastructure | Add inviteCode GSI to LearnfyraClasses CDK construct | Ensure the InviteCodeIndex GSI is provisioned in CDK. This is a blocker for join code lookup. A scan-based fallback is not acceptable. | M5-INF-001 | 1 | P1 |
| M5-INF-008 | Infrastructure | Update SETUP-002 bootstrap script for M05 tables | Update scripts/bootstrap-local-db.js to create all five M05 tables in DynamoDB local (LearnfyraClasses, LearnfyraAssignments, LearnfyraStudentAssignmentStatus, LearnfyraParentChildLinks, LearnfyraParentInviteCodes) with all GSIs as defined in the FRD data model section. | None | 3 | P1 |
| M5-INF-009 | Infrastructure | Add Lambda function for classHandler in CDK stack | Define the learnfyra-class-{env} Lambda function in CDK: ARM_64, Node 18, memory 256MB, timeout 15s, environment variables for all five M05 table names. | M5-INF-001 through M5-INF-005 | 2 | P1 |
| M5-INF-010 | Infrastructure | Add Lambda function for assignmentHandler in CDK stack | Define the learnfyra-assignment-{env} Lambda function in CDK: ARM_64, Node 18, memory 256MB, timeout 15s. Grant read access to Worksheet table and read/write access to Assignment and StudentAssignmentStatus tables. | M5-INF-002, M5-INF-003 | 2 | P1 |
| M5-INF-011 | Infrastructure | Add Lambda function for reviewHandler in CDK stack | Define the learnfyra-review-{env} Lambda function in CDK: ARM_64, Node 18, memory 256MB, timeout 15s. Grant read/write access to ReviewQueueItems table, WorksheetAttempt table, UserProgress table, and StudentAssignmentStatus table. | M5-INF-003, M5-INF-006 | 2 | P1 |
| M5-INF-012 | Infrastructure | Add Lambda function for analyticsHandler in CDK stack | Define the learnfyra-analytics-{env} Lambda function in CDK: ARM_64, Node 18, memory 512MB, timeout 15s (analytics aggregation may be compute-intensive for large classes). Grant read access to StudentAssignmentStatus, UserProgress, and Class tables. | M5-INF-001, M5-INF-003 | 2 | P1 |
| M5-INF-013 | Infrastructure | Add Lambda function for parentHandler in CDK stack | Define the learnfyra-parent-{env} Lambda function in CDK: ARM_64, Node 18, memory 256MB, timeout 15s. Grant read/write access to ParentChildLink and ParentInviteCode tables. Grant read access to StudentAssignmentStatus and UserProgress tables. | M5-INF-004, M5-INF-005 | 2 | P1 |
| M5-INF-014 | Infrastructure | Add API Gateway routes for all M05 endpoints | Wire all Teacher, Parent, and new Student endpoints defined in FRD Section 9 to their respective Lambda functions in the CDK API Gateway construct. Configure Lambda Authorizer on all routes except any public-facing previews. | M5-INF-009 through M5-INF-013 | 5 | P1 |
| M5-INF-015 | Infrastructure | Provision ReviewQueueItems-{env} DynamoDB table | Create the ReviewQueueItem table: PK = REVIEW#{reviewId}, SK = METADATA. Attributes: reviewId, assignmentId, classId, studentId, attemptId, questionNumber, questionText, studentAnswer, expectedAnswer, systemConfidenceScore, currentScore, pointsPossible, status (pending/resolved), createdAt, resolvedAt (nullable), resolvedBy (nullable), overrideScore (nullable). GSI: ClassPendingIndex (PK: classId, SK: status, KEYS_ONLY projection — filter for status = "pending"). | None | 3 | P1 |
| M5-INF-016 | Infrastructure | Add CloudWatch alarms for M05 Lambda functions | Add error rate and p99 latency alarms for all five M05 Lambda functions in the CDK monitoring construct. Error rate threshold: > 1%. Latency threshold: > 2s for analytics, > 500ms for all others. | M5-INF-009 through M5-INF-013 | 3 | P2 |

---

## Backend — Lambda Functions & API Tasks

| Task ID | Layer | Title | Description | Depends On | Estimate | Priority |
|---|---|---|---|---|---|---|
| M5-BE-001 | Backend | Implement classHandler — POST /classes | Create class with unique invite code generation (6-char, excluding O, 0, I, 1), collision check via InviteCodeIndex GSI, write to Class table. Return classId and inviteCode. | M5-INF-001, M5-INF-007 | 5 | P1 |
| M5-BE-002 | Backend | Implement classHandler — GET /classes | List all active classes owned by the authenticated teacher via TeacherIndex GSI. Include pendingReviewCount per class (count of pending ReviewQueueItems for each class). | M5-INF-001, M5-INF-015 | 3 | P1 |
| M5-BE-003 | Backend | Implement classHandler — GET /classes/{classId} | Return class detail for the authenticated teacher's class. Verify teacherId = JWT sub. | M5-INF-001 | 2 | P1 |
| M5-BE-004 | Backend | Implement classHandler — PATCH /classes/{classId} | Update accuracyThreshold or className for the owning teacher's class. | M5-INF-001 | 2 | P2 |
| M5-BE-005 | Backend | Implement classHandler — DELETE /classes/{classId}/archive | Set class status = "archived" and archivedAt timestamp. Verify ownership. Preserve all child records. | M5-INF-001 | 3 | P1 |
| M5-BE-006 | Backend | Implement classHandler — POST /classes/{classId}/invite | Generate a new unique invite code for the class and update the Class record. Verify ownership. Previous code becomes invalid immediately. | M5-INF-001, M5-INF-007 | 3 | P1 |
| M5-BE-007 | Backend | Implement classHandler — GET /classes/{classId}/students | Return roster with assignmentsSummary per student. Query ClassMembership (or the StudentAssignmentStatus table via ClassAssignmentIndex) for completion totals. Verify ownership. | M5-INF-001, M5-INF-003 | 5 | P1 |
| M5-BE-008 | Backend | Implement classHandler — DELETE /classes/{classId}/students/{studentId} | Set ClassMembership status = "removed". Decrement studentCount. Preserve WorksheetAttempt and StudentAssignmentStatus records. Verify ownership. Verify student is in class. | M5-INF-001 | 3 | P1 |
| M5-BE-009 | Backend | Implement classHandler — POST /classes/{classId}/students/{studentId}/parent-invite | Generate teacher-initiated ParentInviteCode record for a specific enrolled student. Verify teacher owns the class and student is enrolled. Set TTL = now + 48h. | M5-INF-001, M5-INF-005 | 3 | P1 |
| M5-BE-010 | Backend | Implement student join endpoint — POST /student/classes/join | Look up class by invite code via InviteCodeIndex GSI. Check for existing active/removed membership. Write ClassMembership record. Increment studentCount. Write StudentAssignmentStatus records for all active assignments in the class. | M5-INF-001, M5-INF-002, M5-INF-003 | 8 | P1 |
| M5-BE-011 | Backend | Implement worksheet library — POST /teacher/library | Save worksheetId to teacher's library. Update Worksheet record with savedBy and savedAt. Verify worksheet exists. | M5-INF-001 (auth); requires Worksheet table write | 3 | P1 |
| M5-BE-012 | Backend | Implement worksheet library — GET /teacher/library | Return all worksheets saved by the authenticated teacher, sorted by savedAt descending. Query by savedBy field (requires GSI on Worksheet table — confirm with M02/M03 whether this GSI exists; if not, add to M5-INF tasks). | M5-BE-011 | 3 | P1 |
| M5-BE-013 | Backend | Implement worksheet library — DELETE /teacher/library/{worksheetId} | Remove savedBy association for the teacher. Do not delete the Worksheet record. Do not affect existing Assignments. | M5-BE-011 | 2 | P2 |
| M5-BE-014 | Backend | Implement assignmentHandler — POST /assignments | Create Assignment record. Verify teacher owns the class. Verify worksheetId exists. Validate all configuration fields (mode, retakePolicy, openAt/closeAt consistency). Write StudentAssignmentStatus records for all currently enrolled students. No AI generation call. | M5-INF-002, M5-INF-003, M5-BE-010 | 8 | P1 |
| M5-BE-015 | Backend | Implement assignmentHandler — GET /assignments/{assignmentId} | Return assignment detail. Verify Assignment.teacherId = JWT sub. | M5-INF-002 | 2 | P1 |
| M5-BE-016 | Backend | Implement assignmentHandler — GET /classes/{classId}/assignments | Return all assignments for a class, sorted by dueDate ascending. Verify class ownership. Include submissionCount and totalStudents per assignment from StudentAssignmentStatus. | M5-INF-002, M5-INF-003 | 5 | P1 |
| M5-BE-017 | Backend | Implement assignmentHandler — PATCH /assignments/{assignmentId} | Update assignment configuration fields. Reject if Assignment.openAt is in the past (return 409 ASSIGNMENT_ALREADY_OPEN). Verify ownership. | M5-INF-002 | 3 | P2 |
| M5-BE-018 | Backend | Implement assignmentHandler — DELETE /assignments/{assignmentId}/close | Set assignment status = "closed". Update all StudentAssignmentStatus records with status = "not-started" or "in-progress" to status = "overdue". Return count of students marked overdue. Verify ownership. | M5-INF-002, M5-INF-003 | 5 | P1 |
| M5-BE-019 | Backend | Implement student assignment endpoints — GET /student/assignments | Return all StudentAssignmentStatus records for the authenticated student via StudentIndex GSI. Join with Assignment records for titles and configuration. Filter out archived class assignments. | M5-INF-002, M5-INF-003 | 5 | P1 |
| M5-BE-020 | Backend | Implement student assignment endpoints — GET /student/assignments/{assignmentId} | Return single assignment detail for the student. Verify StudentAssignmentStatus record exists for studentId + assignmentId. Enforce availability window (openAt/closeAt). | M5-INF-002, M5-INF-003 | 3 | P1 |
| M5-BE-021 | Backend | Integrate assignmentId into M04 solve session start | Update the solve session start logic (M04 solveHandler) to accept an optional assignmentId query parameter. When provided, read the Assignment record to enforce mode, timeLimit, and retakePolicy before creating the WorksheetAttempt. Update the StudentAssignmentStatus record to status = "in-progress" at session start. | M5-INF-002, M5-INF-003; M04 solveHandler | 8 | P1 |
| M5-BE-022 | Backend | Integrate StudentAssignmentStatus update into M04 submit flow | Update the M04 submitHandler to write the StudentAssignmentStatus record to status = "submitted" with score and submittedAt on successful submission, when the attempt has an associated assignmentId. This requires the WorksheetAttempt record to carry the assignmentId field. | M5-INF-003; M04 submitHandler | 5 | P1 |
| M5-BE-023 | Backend | Implement reviewHandler — GET /classes/{classId}/review-queue | Return all pending ReviewQueueItems for the class via ClassPendingIndex GSI. Verify class ownership. Include pendingCount in response. | M5-INF-015 | 3 | P1 |
| M5-BE-024 | Backend | Implement reviewHandler — POST /review-queue/{reviewId}/resolve | Accept action = "approve" or "override". Verify class ownership. Perform cascade: mark ReviewQueueItem resolved, recalculate WorksheetAttempt total score, update UserProgress, update StudentAssignmentStatus. Reject if already resolved (409). | M5-INF-003, M5-INF-015 | 8 | P1 |
| M5-BE-025 | Backend | Integrate review queue item creation into M03 scoring engine | Update the M03 scoring engine to write a ReviewQueueItem record when a short-answer fuzzy-match confidence score is below 0.75 (configurable via Config table). The ReviewQueueItem must include classId and assignmentId, which requires the scoring engine to receive the Assignment context at submission time. | M5-INF-015; M03 scorer | 5 | P1 |
| M5-BE-026 | Backend | Implement analyticsHandler — GET /classes/{classId}/analytics | Return class overview panel: averageScorePerAssignment, completionRate, weakestTopics (up to 5), studentsBelowThreshold. All queries via GSIs. No scans. Verify class ownership. Response must meet 300ms SLA for 200 students. | M5-INF-001, M5-INF-002, M5-INF-003 | 8 | P1 |
| M5-BE-027 | Backend | Implement analyticsHandler — GET /classes/{classId}/analytics/heatmap | Return two-dimensional topic accuracy matrix: rows = students, columns = topics, cells = accuracy percentage or null. Read UserProgress records per studentId. Verify class ownership. Response must meet 300ms SLA for 200 students. | M5-INF-001, M5-INF-003 | 8 | P1 |
| M5-BE-028 | Backend | Implement analyticsHandler — GET /classes/{classId}/students/{studentId}/progress | Return per-student progress view matching M04 GET /student/progress structure. Verify teacher owns class and student is enrolled in class. | M5-INF-001; M04 progress data model | 3 | P1 |
| M5-BE-029 | Backend | Implement parentHandler — POST /parent/link | Validate invite code: record exists, used = false, expiresAt in the future (application-level check, not relying solely on TTL). Atomically set used = true (conditional update on used = false to prevent race condition). Write ParentChildLink record (primary + GSI record). Return child's basic profile. | M5-INF-004, M5-INF-005 | 8 | P1 |
| M5-BE-030 | Backend | Implement parentHandler — GET /parent/children | Return all active ParentChildLink records for the authenticated parent. Query by parentId (primary key). | M5-INF-004 | 2 | P1 |
| M5-BE-031 | Backend | Implement parentHandler — DELETE /parent/children/{studentId} | Set ParentChildLink status = "revoked" with revokedAt. Verify link exists. Return 403 CHILD_NOT_LINKED if no active link. | M5-INF-004 | 2 | P1 |
| M5-BE-032 | Backend | Implement parentHandler — GET /parent/children/{studentId}/progress | Verify active ParentChildLink. Return activity summary (last 7 and 30 days), overallAccuracy, and needsAttention topics (accuracy < 60% across 3+ attempts) from UserProgress records. Return 403 CHILD_NOT_LINKED (not 404) if no active link. | M5-INF-004, M5-INF-006 | 5 | P1 |
| M5-BE-033 | Backend | Implement parentHandler — GET /parent/children/{studentId}/assignments | Verify active ParentChildLink. Read StudentAssignmentStatus records for studentId via StudentIndex GSI. Join with Assignment records for title, class name, and teacher name. Evaluate overdue status lazily (dueDate < now AND status != submitted). Return 403 CHILD_NOT_LINKED if no active link. | M5-INF-002, M5-INF-003, M5-INF-004 | 5 | P1 |
| M5-BE-034 | Backend | Implement student parent invite endpoint — POST /student/parent-invite | Generate ParentInviteCode for the authenticated student's own account. Set TTL = now + 172800 seconds (48 hours). Invalidate any existing unused code for this student (set used = true) before writing the new one. | M5-INF-005 | 3 | P1 |
| M5-BE-035 | Backend | Implement POST /user/role/upgrade endpoint | Allow a user with role = "student" to upgrade to "teacher" or "parent". Write the new role to the Users table. Update Cognito custom attribute if applicable. Reject role downgrade from teacher to student. | M01 auth infrastructure | 3 | P1 |
| M5-BE-036 | Backend | Validate assignmentId field on WorksheetAttempt records | Confirm that the WorksheetAttempt table schema (M04) includes an assignmentId field. If absent, add it as a nullable String field. This is required for StudentAssignmentStatus integration (M5-BE-022) and review queue cascade (M5-BE-024). | M04 WorksheetAttempt schema | 2 | P1 |

---

## Frontend — UI Component Tasks

| Task ID | Layer | Title | Description | Depends On | Estimate | Priority |
|---|---|---|---|---|---|---|
| M5-FE-001 | Frontend | Teacher dashboard shell and class switcher | Build the top-level teacher dashboard layout in learnfyra-app with a class switcher component. The class list is populated from GET /classes. The dashboard must show a prominent review queue badge per class. Blocked on UI template. | M5-BE-002; UI template received | 5 | P1 |
| M5-FE-002 | Frontend | Class creation form | Build the "Create New Class" form with className, gradeLevel, and subjects fields. On submit, call POST /classes. Display the generated inviteCode prominently with a copy-to-clipboard action. | M5-BE-001; UI template | 3 | P1 |
| M5-FE-003 | Frontend | Class roster view | Build the class roster table: studentName, joinedAt, assignmentsSummary, lastActiveAt, overallAccuracy. Include a "Remove Student" action. Include a "Generate Parent Invite" action per student row. Populated from GET /classes/{classId}/students. | M5-BE-007, M5-BE-009; UI template | 5 | P1 |
| M5-FE-004 | Frontend | Worksheet library view and assignment creation form | Build the teacher worksheet library view (GET /teacher/library) with a save and remove action per worksheet. Build the assignment creation form with all configuration fields: mode, timeLimit, dueDate, openAt, closeAt, retakePolicy, retakeLimit. Call POST /assignments on submit. | M5-BE-011, M5-BE-012, M5-BE-014; UI template | 8 | P1 |
| M5-FE-005 | Frontend | Class assignments list view | Build the teacher's assignment list for a class (GET /classes/{classId}/assignments) showing title, mode, dueDate, status, submissionCount/totalStudents. Include a "Close Assignment" action calling DELETE /assignments/{assignmentId}/close. | M5-BE-016, M5-BE-018; UI template | 5 | P1 |
| M5-FE-006 | Frontend | Short-answer review queue view | Build the review queue UI for teachers. Display pending items from GET /classes/{classId}/review-queue with question, student answer, expected answer, and confidence score. Provide "Approve" and "Override Score" actions calling POST /review-queue/{reviewId}/resolve. This view must be reachable from the primary dashboard without sub-menu navigation. | M5-BE-023, M5-BE-024; UI template | 8 | P1 |
| M5-FE-007 | Frontend | Class analytics overview panel | Build the class overview analytics panel: averageScorePerAssignment bar chart or table, overallCompletionRate indicator, weakestTopics list, studentsBelowThreshold list. Populated from GET /classes/{classId}/analytics. Accuracy threshold must be editable inline via PATCH /classes/{classId}. | M5-BE-026, M5-BE-004; UI template | 8 | P1 |
| M5-FE-008 | Frontend | Topic-by-student accuracy heatmap | Build the heatmap component: rows = students, columns = curriculum topics, cells colour-coded red to green by accuracy percentage, null cells rendered as grey. Clicking a cell opens the student drill-down. Populated from GET /classes/{classId}/analytics/heatmap. | M5-BE-027; UI template | 8 | P1 |
| M5-FE-009 | Frontend | Student drill-down progress view (teacher view) | Build the read-only per-student progress view accessible from the heatmap cell click. Populated from GET /classes/{classId}/students/{studentId}/progress. This is a read-only mirror of the student's own M04 progress view — reuse M04 progress components where possible. | M5-BE-028; M04 progress UI; UI template | 3 | P1 |
| M5-FE-010 | Frontend | Student class join flow | Build the "Join a Class" form for students with a 6-character invite code input field. On submit, call POST /student/classes/join. Display confirmation with class name and teacher name on success. | M5-BE-010; UI template | 3 | P1 |
| M5-FE-011 | Frontend | Student assignment list view | Build the student assignment list populated from GET /student/assignments. Show title, className, mode, dueDate, status per assignment. Each assignment links to the solve flow (M03). Overdue items must be visually distinct. | M5-BE-019; UI template | 5 | P1 |
| M5-FE-012 | Frontend | Student parent invite code generation | Add a "Generate Parent Invite Code" option to the student's profile settings. Call POST /student/parent-invite. Display the invite code and expiry timestamp with a copy-to-clipboard action. | M5-BE-034; UI template | 2 | P1 |
| M5-FE-013 | Frontend | Parent dashboard shell and child switcher | Build the top-level parent dashboard layout in learnfyra-app with a child-switcher component at the top level (not in a sub-menu). The child list is populated from GET /parent/children. Blocked on UI template. | M5-BE-030; UI template | 5 | P1 |
| M5-FE-014 | Frontend | Parent link to child form | Build the "Link to a Child" form for parents with an invite code input. Call POST /parent/link. Handle all error cases: INVITE_CODE_NOT_FOUND (404), INVITE_CODE_ALREADY_USED (409), INVITE_CODE_EXPIRED (410) with user-friendly messages. | M5-BE-029; UI template | 3 | P1 |
| M5-FE-015 | Frontend | Parent child progress dashboard | Build the parent read-only progress view for a linked child: activity summary (last 7 and 30 days), assignment status list (from GET /parent/children/{studentId}/assignments), and needsAttention topic list. Must surface "no active assignments" state gracefully. | M5-BE-032, M5-BE-033; UI template | 8 | P1 |
| M5-FE-016 | Frontend | Role upgrade and registration flow updates | Update the registration and profile settings pages in learnfyra-app to include teacher and parent role selection. Call POST /user/role/upgrade for existing users. Blocked on UI template. | M5-BE-035; UI template | 3 | P1 |

---

## Auth & RBAC Tasks

| Task ID | Layer | Title | Description | Depends On | Estimate | Priority |
|---|---|---|---|---|---|---|
| M5-AUTH-001 | Auth & RBAC | Add "teacher" and "parent" role values to Lambda Authorizer | Update the Lambda Authorizer (M01) to support role = "teacher" and role = "parent" JWT claims. Verify that role-based route restrictions work for all M05 teacher-only and parent-only routes. | M01 Lambda Authorizer | 3 | P1 |
| M5-AUTH-002 | Auth & RBAC | Implement per-class ownership check utility | Create a reusable ownership check function (reads Class.teacherId and compares to JWT sub) that is called at the handler layer in every class-scoped endpoint. This must not be duplicated per handler — a shared utility prevents drift. | M5-INF-001 | 2 | P1 |
| M5-AUTH-003 | Auth & RBAC | Implement parent-child link verification utility | Create a reusable RBAC check function that queries the ParentChildLink table by parentId and childId and verifies status = "active". Returns 403 CHILD_NOT_LINKED for any non-matching or revoked link. This utility must be called in every parent endpoint before any data access. Return 403 (not 404) for enumeration prevention. | M5-INF-004 | 2 | P1 |
| M5-AUTH-004 | Auth & RBAC | Implement invite code atomic consumption | Implement the conditional DynamoDB update (ConditionExpression: used = false) for POST /parent/link and POST /student/parent-invite code invalidation. The condition prevents two simultaneous requests from both consuming the same code. Handle ConditionalCheckFailedException and return 409 INVITE_CODE_ALREADY_USED. | M5-INF-005 | 3 | P1 |
| M5-AUTH-005 | Auth & RBAC | Implement student assignment access control | Create a handler-level check that verifies a StudentAssignmentStatus record exists for the authenticated studentId before allowing access to any specific assignment. Enforce openAt/closeAt window. Return 403 (not 404) for assignment IDs not assigned to the student to prevent enumeration. | M5-INF-003 | 2 | P1 |
| M5-AUTH-006 | Auth & RBAC | Verify Cognito custom attribute supports teacher and parent roles | Confirm Cognito User Pool custom attribute (e.g., custom:role) supports "teacher" and "parent" as valid values. If the attribute is currently an enum restricted to "student" and "admin", update the Cognito CDK construct. This must be confirmed before M5-BE-035 is implemented. | M01 Cognito CDK construct | 2 | P1 |

---

## Testing Tasks

| Task ID | Layer | Title | Description | Depends On | Estimate | Priority |
|---|---|---|---|---|---|---|
| M5-TEST-001 | Testing | Unit tests — classHandler | Test all CRUD operations for class management: create class, list classes, get class, archive class, regenerate invite code, remove student. Test ownership checks (403 for non-owner). Test invite code uniqueness and collision regeneration. Test boundary cases: className at 1 char, at 100 chars, at 101 chars (reject). | M5-BE-001 through M5-BE-009 | 5 | P1 |
| M5-TEST-002 | Testing | Unit tests — student join flow | Test POST /student/classes/join: valid join, invalid code (404), already enrolled (409), removed student rejoining (200, updates existing record). Test StudentAssignmentStatus record creation for active assignments. Test inviteCode GSI lookup. | M5-BE-010 | 3 | P1 |
| M5-TEST-003 | Testing | Unit tests — worksheet library handler | Test save, list, and remove library operations. Test that remove does not delete Assignment records. Test that a non-owner cannot save to another teacher's library. | M5-BE-011 through M5-BE-013 | 3 | P2 |
| M5-TEST-004 | Testing | Unit tests — assignmentHandler | Test assignment creation with all configuration combinations: mode, timeLimit, dueDate, openAt/closeAt, retakePolicy. Test validation: openAt must be before closeAt, timeLimit minimum 60s, retakeLimit required when retakePolicy = "limited". Test StudentAssignmentStatus record creation for all enrolled students. Test PATCH rejection after openAt. Test close and overdue cascade. | M5-BE-014 through M5-BE-018 | 8 | P1 |
| M5-TEST-005 | Testing | Unit tests — solve session enforcement | Test that mode = "test" hides per-question feedback. Test timeLimit enforcement and auto-submit. Test retakePolicy = "once" rejection after first submission. Test retakePolicy = "limited" rejection after retakeLimit reached. Test availability window (openAt/closeAt) enforcement. | M5-BE-021, M5-BE-022 | 5 | P1 |
| M5-TEST-006 | Testing | Unit tests — reviewHandler | Test review queue listing (pending only). Test approve action (no score change). Test override action: verify cascade to WorksheetAttempt, UserProgress, and StudentAssignmentStatus. Test rejection of override for another teacher's class (403). Test rejection of resolve on already-resolved item (409). | M5-BE-023, M5-BE-024 | 5 | P1 |
| M5-TEST-007 | Testing | Unit tests — review queue item creation in scoring engine | Test that a ReviewQueueItem is written when fuzzy-match confidence is below 0.75. Test that no ReviewQueueItem is written when confidence is at or above 0.75. Test that ReviewQueueItem includes classId and assignmentId from Assignment context. | M5-BE-025 | 3 | P1 |
| M5-TEST-008 | Testing | Unit tests — analyticsHandler | Test class overview panel: correct averageScore, completionRate, weakestTopics (ranked by accuracy), studentsBelowThreshold (at 60% default and at custom threshold). Test heatmap: correct matrix dimensions, null cells for topics with no data. Test 403 for non-owner teacher. Test boundary: class with 1 student, class with 200 students (performance assertion). | M5-BE-026, M5-BE-027, M5-BE-028 | 8 | P1 |
| M5-TEST-009 | Testing | Unit tests — parentHandler | Test POST /parent/link: valid code, expired code (410), already-used code (409), not-found code (404). Test atomic consumption (simulate concurrent requests). Test GET /parent/children (list). Test DELETE /parent/children/{studentId} (revoke). Test GET /parent/children/{studentId}/progress: correct last7Days/last30Days, correct needsAttention (threshold 60%, 3+ attempts). Test GET /parent/children/{studentId}/assignments: correct status, lazy overdue evaluation. Test 403 CHILD_NOT_LINKED for all unlinked studentId requests. | M5-BE-029 through M5-BE-033 | 8 | P1 |
| M5-TEST-010 | Testing | Unit tests — student parent invite and role upgrade | Test POST /student/parent-invite: code generated, TTL set, previous code invalidated. Test POST /user/role/upgrade: student to teacher, student to parent, reject teacher to student downgrade. | M5-BE-034, M5-BE-035 | 3 | P1 |
| M5-TEST-011 | Testing | Unit tests — RBAC utilities | Test per-class ownership check utility with matching teacher (pass) and non-matching teacher (403). Test parent-child link utility with active link (pass), revoked link (403), no link (403). Test that 403 is returned (not 404) for unlinked child access. | M5-AUTH-002, M5-AUTH-003 | 3 | P1 |
| M5-TEST-012 | Testing | Unit tests — invite code atomic consumption | Test conditional update: first consumption succeeds, second simultaneous consumption returns 409 INVITE_CODE_ALREADY_USED. Test that no ParentChildLink is written on 409. | M5-AUTH-004 | 3 | P1 |
| M5-TEST-013 | Testing | Integration tests — teacher full flow | Full end-to-end flow: teacher creates class, student joins via invite code, teacher creates assignment, student starts solve session (assignment-scoped), student submits attempt, StudentAssignmentStatus updates to "submitted", teacher views analytics with correct completionRate. | M5-BE-001 through M5-BE-022 | 8 | P1 |
| M5-TEST-014 | Testing | Integration tests — review queue full flow | Full cascade: student submits short-answer below confidence threshold, ReviewQueueItem is created, teacher resolves with override, WorksheetAttempt score updates, UserProgress updates, StudentAssignmentStatus score updates. Verify cascade is complete and consistent. | M5-BE-023 through M5-BE-025 | 5 | P1 |
| M5-TEST-015 | Testing | Integration tests — parent linking and dashboard | Full flow for both student-initiated and teacher-initiated flows: code generated, parent consumes code, ParentChildLink written, parent views assignments and progress. Verify 403 on revoked link. Verify expired code returns 410. | M5-BE-029 through M5-BE-034 | 5 | P1 |
| M5-TEST-016 | Testing | CDK assertion tests | Write CDK assertion tests (using @aws-cdk/assertions) for all five M05 DynamoDB tables: verify correct PK/SK, all GSIs present with correct keys, TTL attribute configured on ParentInviteCode table, DynamoDB Streams enabled on UserProgress table. Verify all five Lambda functions are defined with correct memory and timeout. | M5-INF-001 through M5-INF-015 | 5 | P1 |
| M5-TEST-017 | Testing | Boundary case tests — grade and class size | Confirm all teacher analytics endpoints function correctly at Grade 1 (youngest students, minimal progress data) and Grade 10 (maximum grade). Confirm analytics endpoints return within SLA with 5 students (minimum meaningful class) and 200 students (SLA boundary). Confirm assignment creation works with 5 questions and 30 questions. | M5-BE-026, M5-BE-027 | 3 | P1 |
| M5-TEST-018 | Testing | Security tests — enumeration prevention | Verify that GET /parent/children/{studentId}/progress, GET /parent/children/{studentId}/assignments, and GET /student/assignments/{assignmentId} all return 403 (not 404) for IDs that exist in DynamoDB but are not linked to the authenticated user. Verify response body is identical for existing-but-unlinked vs non-existent IDs to prevent enumeration. | M5-AUTH-003, M5-AUTH-005 | 3 | P1 |

---

## Dependency Graph

The following sequential and parallel constraints govern task execution.

**Foundation (must complete before any other M05 work can start):**
- M5-INF-001, M5-INF-002, M5-INF-003, M5-INF-004, M5-INF-005, M5-INF-015 can all be parallelised — they are independent table provisions.
- M5-INF-006 (DynamoDB Streams on UserProgress) depends on confirming M04 CDK construct state. Must complete before M5-BE-032 and M5-BE-024 are deployed.
- M5-INF-007 (InviteCodeIndex) is part of M5-INF-001 and should be delivered together.
- M5-INF-008 (bootstrap script) must complete before any local development testing begins.
- M5-AUTH-006 (Cognito role support) must complete before M5-BE-035 and M5-AUTH-001 can be tested.

**Backend dependencies (sequential where noted):**
- M5-BE-001 through M5-BE-009 (class management) depend on M5-INF-001.
- M5-BE-010 (student join) depends on M5-INF-001, M5-INF-002, and M5-INF-003 because joining triggers StudentAssignmentStatus creation.
- M5-BE-014 (assignment creation) depends on M5-BE-010 being defined first — the assignment creation must know the StudentAssignmentStatus write pattern established for the join flow.
- M5-BE-021 and M5-BE-022 (solve session integration) must complete before M5-BE-025 (review queue item creation) because the review queue needs the assignmentId from the WorksheetAttempt record.
- M5-BE-023 and M5-BE-024 (review resolution) depend on M5-BE-025 (review item creation) being implemented first — no items means no queue to test.
- M5-BE-026 and M5-BE-027 (analytics) depend on M5-BE-014 and M5-BE-022 — analytics are only meaningful when StudentAssignmentStatus records are being populated.
- M5-BE-029 through M5-BE-034 (parent flows) are independent of teacher flows and can begin in parallel once M5-INF-004 and M5-INF-005 are complete.
- M5-BE-036 (WorksheetAttempt assignmentId field) must complete before M5-BE-021 and M5-BE-022.

**Frontend dependencies:**
- All frontend tasks are blocked on UI template receipt.
- Once unblocked, M5-FE-001 (teacher dashboard shell) and M5-FE-013 (parent dashboard shell) can be parallelised.
- M5-FE-004 (worksheet library and assignment creation) depends on M5-FE-001 (teacher shell) being complete for navigation context.
- M5-FE-007 and M5-FE-008 (analytics panel and heatmap) depend on M5-FE-005 (assignment list) being complete — they share the class context layout.
- M5-FE-015 (parent child progress dashboard) depends on M5-FE-013 (parent shell) and M5-FE-014 (parent link form) being complete.

**Testing dependencies:**
- All unit tests depend on the corresponding backend task completing.
- M5-TEST-013 through M5-TEST-015 (integration tests) depend on all backend tasks in their respective flows completing.
- M5-TEST-016 (CDK tests) depends on all INF tasks completing.
- M5-TEST-017 and M5-TEST-018 are test-only tasks that can run once backend tasks are complete — no additional implementation required.

**Parallelisable groups:**
- Group 1 (parallel): M5-INF-001, M5-INF-002, M5-INF-003, M5-INF-004, M5-INF-005, M5-INF-015
- Group 2 (parallel, after Group 1): M5-BE-001 through M5-BE-009 (class management), M5-BE-029 through M5-BE-031 (parent linking — independent of teacher backend)
- Group 3 (parallel, after Group 2): M5-BE-014 through M5-BE-018 (assignment management), M5-BE-023 through M5-BE-025 (review queue creation)
- Group 4 (parallel, after Group 3): M5-BE-026, M5-BE-027, M5-BE-028 (analytics), M5-BE-032, M5-BE-033 (parent dashboards)
- Group 5 (parallel, after UI template received and Group 2 done): All M5-FE tasks, split between frontend and backend engineers
